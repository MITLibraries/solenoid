dist: xenial
language: python
services:
  - postgresql
python: 3.8
env:
  - PIPENV_IGNORE_VIRTUALENVS=1 PIPENV_PYUP_API_KEY=""
before_install: pip install pipenv coveralls
install: pipenv install --dev
jobs:
  include:
    - name: Tests
      script: pipenv run coverage run manage.py test
      after_success: coveralls
    - name: "PEP 508"
      script: pipenv check
